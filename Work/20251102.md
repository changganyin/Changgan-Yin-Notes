### 6 个 section 的内容都放在了 /sec/ 目录下

**每一节的内容：一部分是参考别人文章然后修改过的，一部分是自己写然后给 ChatGPT 润色过的**

#### 0_abstract.tex
Embodied VLM agents 效果很好，被大面积应用 -> 当前还存在很多安全隐患没有被研究 -> 我们系统性地研究了这些隐患 -> 研究结果表明 XXX
#### 1_intro.tex
分了两段
右上角加个图，一分为二，传统|新的

- 第一段介绍基于 **MLLMs 的 Embodied VLM agents**，功能很强效果很好，但是它的底层逻辑和普通 VLM 一样，Prompt injection 也能被迁移过去，新场景 embodied agents (Action Hijacking)；介绍 Prompt injection，在 LLM 和 VLM 上都试过了并且很有效，但是对 VLM-based agents 的鲁棒性不够深入理解，对 semantic and API levels 的认知有限 (和第一段放一块)
- 第三段说我们补全了这个研究点，系统性地进行了研究，然后做出了一些贡献
    
#### 2_related_work.tex
分了三个小节
- 第一节还是 ~~MLLMs，性能很强但是易受攻击~~ Embodied Vision-Language Model agent
- 第二节是 Prompt injection，把*之前看过的各种攻击方法*都写上去了： GCG, MGCG, NeuralExec, JudgeDeceiver, PGD, VAJM, FigStep, SI-attack, CS-DJ, JOOD，每个攻击方法用一两句话简单说明了一下，然后每个方法后面添加对应文章的引用 （分类，每一类标号，要有条理）
- ~~第三节是 Defense Mechanisms，把其他文章里提到的防御方法都写上去了：CIDER, JailGuard, LVLM-LP, VLGuard，每个防御方法用一两句话简单说明了一下，然后每个方法后面添加对应文章的引用~~
    改成对 **VLM agents 的鲁棒性的研究**，unclear，需要我们研究
#### 3_methodology.tex
分了两个小节，每一节的内容就是把 notion 里的公式和说明抄上去了
- 第一节是 Semantic Injection (High-Level)
- 第二节是 Explicit API Injection (Low-Level)
不要写着写着就提到意义和重要性
#### 4_experiment.tex
把 notion 里的东西全抄上去了：Models, Datasets, Metrics, Attack Methods，最后再加一小段文字声称自己的测试环境全面而可靠
#### 5_conclusion.tex
还没写




### 引用的文章信息都放在了 main.bib 里
引用的信息是从 Google Scholar 里复制的（文章下面的引用 -> BibTex -> 全部复制），例如
**全部格式要一样，page 可能没有，year 一定要有**
引用去 *DBLP* 上找
```BibTex
@article{chakraborty2024cross,
  title={Cross-modal safety alignment: Is textual unlearning all you need?},
  author={Chakraborty, Trishna and Shayegani, Erfan and Cai, Zikui and Abu-Ghazaleh, Nael and Asif, M Salman and Dong, Yue and Roy-Chowdhury, Amit K and Song, Chengyu},
  journal={arXiv preprint arXiv:2406.02575},
  year={2024}
}
```

具体引用的文章：
- 参考了其他人的文章，例如别人在 MLLM 这个词后面加了引用了一些模型的文章，我就也在这里引用同样的文章（只看了这些文章的摘要，没有看具体内容）
- 第一次提到某个东西，例如 embodied agents，我去 Google Scholar 上搜这个关键词，然后找几个看上去比较好的文章（只看了摘要，优先选发在顶刊上的），再在这里引用

### 数学公式的格式问题
![[Pasted image 20251101204013.png]]
如图所示，该公式长度较长，如果一行摊开的话长度会超过页面宽度的一半，应该怎么处理？

**直接写复杂和详细的那个公式，不要连着几个公式**

每一行公式后面要加标点,.
```LaTeX
\begin{equation}
\begin{aligned}
    \tilde{x}^* = &\ \arg\max_{\tilde{x}} \log P(T \mid \tilde{x}) \\
    = &\ \arg\max_{\tilde{x}} \sum_{k=1}^K \log P(t_k \mid \tilde{x}, t_{<k}) \\
    &\quad \text{s.t.} \quad \|\tilde{x} - x\|_p \leq \epsilon
\end{aligned}
\label{eq:lowlevel-wb-image}
\end{equation}
```
我的解决方案为在这里加一个 aligned 的对齐，但是这样总感觉看着很奇怪
